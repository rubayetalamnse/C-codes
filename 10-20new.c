#include <stdio.h>
int main()
{
    int i = 0;
    while (i <= 20)
    {
        if (i >= 10)
        {
            printf("\nThe value of i is : %d", i);
           
        }
        
    }
    return 0;
}