#include <stdio.h>
int main()
{
    float radius = 3;
    float pi = 3.14;
    printf("the area of cicrle is %f \n", radius * pi * pi);
    int height = 3;
    printf("volume of this circle is %f", pi * radius * radius * height);

    return 0;
}